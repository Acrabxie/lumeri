{
  "identifier": "default",
  "description": "Capability file for Gemia desktop app",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "shell:allow-execute",
    "shell:allow-open",
    "shell:allow-spawn",
    "dialog:default",
    "dialog:allow-open"
  ]
}
